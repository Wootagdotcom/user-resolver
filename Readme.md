user-resolver rest server in Docker
===

This repository provides a rest server to resolve user correspond to video-id
Run
---

```bash
docker run --name user-resolver \
--env PORT=8080\
-d shukla2009/user-resolver
```
